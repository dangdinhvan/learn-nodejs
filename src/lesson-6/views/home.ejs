<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Đề thi bằng lái B2</title>
		<style>
			body {
				font-family: Arial, Helvetica, sans-serif;
			}

			.columns {
				column-count: 6;
				column-gap: 32px;
			}

			.exam-item {
				display: flex;
				justify-content: space-between;
				align-items: center;
				margin-bottom: 20px;
				padding: 6px 12px;
				background-color: rgb(209, 203, 203);
				font-weight: 700;
			}

			button {
				cursor: pointer;
			}
		</style>
	</head>

	<body>
		<h1 style="text-align: center">Đề thi bằng lái xe B2</h1>
		<div class="columns">
			<% exams.forEach((exam,index)=> { %>
			<div class="exam-item">
				<span><%= exam.name %></span
				><button
					onClick="window.location.href=`/detail/<%= exam.id + 1 %>`"
				>
					Thi thử
				</button>
			</div>
			<% }); %>
		</div>
	</body>
</html>
